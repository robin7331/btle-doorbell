{
  "name": "btle-doorbell",
  "version": "0.0.1",
  "description": "This script is listening for a BTLE device which sends a message once the doorbell rings.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Robin Reiter",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^1.14.1",
    "noble": "^1.6.0"
  }
}
